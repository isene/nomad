# fresh

The ten most recently installed apps, newest first.

- Reads install times from PackageManager (`firstInstallTime`).
- User-installed apps only; system apps are filtered out.
- No Rust core, no network, no background work. One screen, read-only.
